# Chapter 8: Headers, CORS, and CSRF

**Demo snapshot** for the Apress book. Continues from `chapter_07`.

What this chapter adds: cors + csrf(fetch-metadata) on catalog/orders.

```sh
pnpm install
pnpm test
pnpm dev
```

Pin: `@daloyjs/core` ^1.0.0. Tag: `chapter-08`.
