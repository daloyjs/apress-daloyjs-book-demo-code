# Chapter 1: Vibe Coding Already Shipped to Production

**Companion snapshot** for *DaloyJS: Contract-First, Secure-by-Default TypeScript APIs* (Apress).

| | |
| --- | --- |
| Continues from | `(none)` |
| Book chapter | manuscript under `otherdocs/apress/` |
| Project | `orders-api` from create-daloy `node-basic` |
| What this chapter adds | Baseline create-daloy node-basic (demo /books still present). Ch1 is threat framing; this is the generator output the book starts from. |

## Run

```sh
pnpm install
pnpm test
pnpm dev
```

Pin: `@daloyjs/core` ^1.0.0, Node 24 LTS or 26+, pnpm 11+, TypeScript 7, Zod 4.

This folder is a **full tree** at the end of the chapter (not a patch). Prefer it or tag `chapter-01`.

