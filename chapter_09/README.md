# Chapter 9: Rate Limiting and Abuse Control

**Demo snapshot** for the Apress book. Continues from `chapter_08`.

What this chapter adds: rateLimit keyGenerator on client IP.

```sh
pnpm install
pnpm test
pnpm dev
```

Pin: `@daloyjs/core` ^1.0.0. Tag: `chapter-09`.
