# Chapter 22: Capstone

**Demo snapshot** for the Apress book. Continues from `chapter_21`.

What this chapter adds: Modular routes; loadShedding; admin mTLS+IP; createWebhookSender; partner httpSignatureAuth; MCP process.

```sh
pnpm install
pnpm test
pnpm dev
```

Pin: `@daloyjs/core` ^1.0.0. Tag: `chapter-22`.
