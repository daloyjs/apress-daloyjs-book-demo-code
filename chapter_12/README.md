# Chapter 12: Outbound Safety

**Demo snapshot** for the Apress book. Continues from `chapter_11`.

What this chapter adds: POST /webhooks/register via fetchGuard.

```sh
pnpm install
pnpm test
pnpm dev
```

Pin: `@daloyjs/core` ^1.0.0. Tag: `chapter-12`.
