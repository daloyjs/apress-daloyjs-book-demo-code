# Chapter 23: Post-capstone complete

**Demo snapshot** for the Apress book. Continues from `chapter_22`.

What this chapter adds: POST /orders/:id/returns module on assembled capstone tree.

```sh
pnpm install
pnpm test
pnpm dev
```

Pin: `@daloyjs/core` ^1.0.0. Tag: `chapter-23`.
