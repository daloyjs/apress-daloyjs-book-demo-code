# Chapter 17: Designing for Coding Agents

**Companion snapshot** for *DaloyJS: Contract-First, Secure-by-Default TypeScript APIs* (Apress).

| | |
| --- | --- |
| Continues from | `chapter_16` |
| Book chapter | manuscript under `otherdocs/apress/` |
| Project | `orders-api` from create-daloy `node-basic` |
| What this chapter adds | AGENTS.md webhook rule for fetchGuard. |

## Run

```sh
pnpm install
pnpm test
pnpm dev
```

Pin: `@daloyjs/core` ^1.0.0, Node 24 LTS or 26+, pnpm 11+, TypeScript 7, Zod 4.

This folder is a **full tree** at the end of the chapter (not a patch). Prefer it or tag `chapter-17`.

