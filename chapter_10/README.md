# Chapter 10: Authentication and Authorization, Safely

**Demo snapshot** for the Apress book. Continues from `chapter_09`.

What this chapter adds: jwk(ES256 demo JWKS) + requireScopes; mintDemoToken tests.

```sh
pnpm install
pnpm test
pnpm dev
```

Pin: `@daloyjs/core` ^1.0.0. Tag: `chapter-10`.
